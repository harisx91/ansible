<!DOCTYPE html>
<html>
<head>
  <title>Dynamic Server Info - {{ ansible_facts.hostname }}</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    .info { background: #f0f0f0; padding: 10px; border-radius: 5px; }
  </style>
</head>
<body>
  <h1>Welcome to {{ ansible_facts.hostname }}</h1>
  <div class="info">
    <p><strong>Hostname:</strong> {{ ansible_facts.hostname }}</p>
    <p><strong>IP Address:</strong> {{ ansible_facts.default_ipv4.address }}</p>
    <p><strong>OS:</strong> {{ ansible_facts.distribution }} {{ ansible_facts.distribution_version }}</p>
    <p><strong>CPU Cores:</strong> {{ ansible_facts.processor_count }}</p>
    <p><strong>Memory (MB):</strong> {{ ansible_facts.memtotal_mb }}</p>
    <p><strong>Deployment Time:</strong> {{ ansible_date_time.iso8601 }}</p>
  </div>
</body>
</html>